@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{WebApp.Settings.Settings.LoginPath}?returnUrl={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}
