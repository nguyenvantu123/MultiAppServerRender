@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{AuthenPath.LoginPath}?returnurl={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}
